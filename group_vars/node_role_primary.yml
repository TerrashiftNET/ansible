---

vhosts:
  admin.terrashift.net:
    php_fpm: "php8.1"
    doc_root: "public/"
  grafana.terrashift.net:
    proxy:
      port: 3000
  map.terrashift.net:
    proxy:
      port: 8100
      ip: "{{ hostvars['host01.terrashift.net']['ansible_eth0'].ipv4.address }}"
  stats.terrashift.net: {}
